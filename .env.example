# ============================================
# CLB Gà Chọi Cao Đổi - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env file to Git!

# Database Configuration
DB_NAME=livestream_db
DB_USERNAME=livestream_user
DB_PASSWORD=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_32
DB_URL=jdbc:postgresql://postgres:5432/livestream_db

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_32

# JWT Configuration (Generate with: openssl rand -base64 64)
JWT_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_64
JWT_EXPIRATION=3600000

# Domain Configuration (Change to your domain)
DOMAIN=yourdomain.com

# CORS Origins (Change to your domain)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Frontend URLs (Change to your domain)
VITE_API_URL=https://yourdomain.com/api
VITE_WS_URL=wss://yourdomain.com/api/ws
VITE_HLS_BASE_URL=https://yourdomain.com/hls

# Stream URLs (Change to your domain)
STREAM_RTMP_URL=rtmp://yourdomain.com:1935/live
STREAM_HLS_BASE_URL=https://yourdomain.com/hls

# ============================================
# HOW TO GENERATE SECURE SECRETS:
# ============================================
# 1. Database Password:
#    openssl rand -base64 32
#
# 2. Redis Password:
#    openssl rand -base64 32
#
# 3. JWT Secret:
#    openssl rand -base64 64
#
# ============================================
